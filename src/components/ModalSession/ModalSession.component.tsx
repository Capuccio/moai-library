/*
export default function ModalSession() {
	return (
		<Modal backdrop="blur" size="md" isDismissable={false} isOpen={showModal} onClose={() => setShowModal(false)}>
      <ModalContent>
        <ModalHeader className="flex items-center justify-center pb-0">
          <IconAlertCircle height={70} width={70} color="#263065" />
        </ModalHeader>
        <ModalBody className="pt-2 px-8 text-center">
          <Typography tag="p" className="text-lg pb-2">
            {t('SessionModal.Title')}
            {': '}
            {modalTimer}
          </Typography>
          <Typography tag="p">{t('SessionModal.Subtitle')}</Typography>
        </ModalBody>
        <ModalFooter className="flex justify-between">
          <Button color="danger" variant="light" onPress={handleCloseSession} style={{ border: '1px solid #F31260' }}>
            {t('SessionModal.ButtonClose')}
          </Button>
          <Button color="primary" onPress={handleStay}>
            {t('SessionModal.ButtonContinue')}
          </Button>
        </ModalFooter>
      </ModalContent>
    </Modal>
	)
}
*/
